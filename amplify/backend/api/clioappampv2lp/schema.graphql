type Todo @model @auth(rules: [{allow: private}]) {
  id: ID!
  name: String!
  description: String
}

type ClioMove @model @auth(rules: [{allow: public}]) {
  id: ID!
  board_fen: String
  movenumber: Int!
  move: String!
} 